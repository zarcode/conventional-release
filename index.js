export const add = (a, b) => a + b;
export const sub = (a, b) => a - b;
export const multiply = (a, b) => a * b;
export const divide = (a, b) => (b != 0 ? a / b : null);
export const nullable = undefined;
export const nullable2 = null;
